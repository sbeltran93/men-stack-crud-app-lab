<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= car.model %></title>
</head>
<body>
  <h1><%= car.year %> <%= car.make %> <%= car.model %></h1>
  <% if(car.isRunning){ %>
    <p>This car runs</p>
  <% } else { %>
    <p>This car needs work </p>
  <% } %>
  <form action="/cars/<%=car._id%>?_method=DELETE" method="POST">
    <button type="submit">
      Delete <%= car._id %>
    </button>
  </form>
  <a href="/cars/<%= car._id %>/edit">Edit <%= car._id %></a>
  <a href="/cars">Back to Cars</a>
</body>
</html>